const WithStyledComponent = (wrappedComponent, backgroundColor) => {
  const WithStyledComponent = (props) => {
    const componentStyle = {
      width: "75%",
      backgroundColor: backgroundColor,
    };
  };
};
